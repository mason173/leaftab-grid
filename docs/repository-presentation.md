# Repository Presentation Kit

Use this page when updating the GitHub repository's public-facing metadata.

## Suggested repository description

Desktop-style shortcut grid engine and React adapters extracted from LeafTab, with folder-aware drag, merge, extraction, and span-aware layout.

## Suggested website URL

`https://mason173.github.io/leaftab-grid/`

## Suggested topics

- `drag-and-drop`
- `grid-layout`
- `react`
- `typescript`
- `ui-components`
- `sortable-grid`
- `desktop-ui`
- `shortcut-grid`
- `folder-ui`
- `browser-start-page`

## Suggested About blurb

Leaftab Grid is the open-source shortcut grid system extracted from LeafTab. It focuses on root-grid reorder, merge-to-folder behavior, folder extraction back into the root surface, and span-aware placement for larger folder tiles.

## Suggested pinned links

- GitHub Pages showcase: `https://mason173.github.io/leaftab-grid/`
- Behavior contract: `https://github.com/mason173/leaftab-grid/blob/main/docs/compact-grid-rules.md`
- LeafTab origin repo: `https://github.com/mason173/LeafTab`

## Suggested social preview copy

Headline:

`Leaftab Grid`

Subheadline:

`Desktop-style shortcut grids extracted from LeafTab`

Support line:

`Root reorder • folder merge • extraction • span-aware layout`

## Suggested launch checklist

1. Enable GitHub Pages with `GitHub Actions` as the source.
2. Confirm `.github/workflows/deploy-pages.yml` has run successfully on `main`.
3. Add the showcase URL to the repository website field.
4. Update the repository description and topics.
5. Optionally upload a custom social preview image that matches the showcase branding.
